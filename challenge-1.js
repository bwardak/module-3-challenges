function isNumberPositive(number) {
    return number >= 0
}

/* code I used to test the output 

let positivetest = isNumberPositive(6)
let negativetest = isNumberPositive(-3)

console.log(positivetest)
console.log(negativetest)*/
